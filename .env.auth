# Sample authentication configuration
# Replace with your actual values

# Replace with your Google OAuth credentials
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
AUTH_SECRET_KEY=your-auth-secret-key

# Replace "example.com" with your actual domain or public IP
# If you're using a different port, include it (example.com:5050)
EXTERNAL_DOMAIN=example.com:5050
SERVER_NAME=example.com:5050

# Authentication settings
AUTH_REQUIRED=true
DEV_MODE=false